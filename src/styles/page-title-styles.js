import {TEXT_BLUE} from "../constants/css-colors";

const PAGE_TITLE_STYLES = {
    color: TEXT_BLUE,
    marginLeft: 10,
    fontSize: 17,
    textAlign: 'center'
};

export default PAGE_TITLE_STYLES;
